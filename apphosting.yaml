
version: 1
runtime: nodejs20

scripts:
  buildCommand: npm ci --legacy-peer-deps && npm run build
  runCommand: npm start

outputFiles:
  serverApp:
    include:
      - ".next"
      - "package.json"
      - "public"
      - "node_modules" 

